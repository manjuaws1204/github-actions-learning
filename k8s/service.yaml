apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: github-actions
  labels:
    app: order-service
spec:
  selector:
    app: order-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
